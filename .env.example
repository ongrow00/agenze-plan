# Front (Vite) — apenas chaves públicas do projeto Supabase
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Segredos da Edge Function `generate-plan` (Dashboard → Edge Functions → Secrets, ou `supabase secrets set`):
#   SUPABASE_URL          — já injetado pelo Supabase em produção
#   SUPABASE_SERVICE_ROLE_KEY
#   GEMINI_API_KEY
